meta {
  name: Get Messages
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/conversations/{{conversationId}}/messages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

query {
  ~since: 0
}
