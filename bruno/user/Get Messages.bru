meta {
  name: Get Messages
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/conversations/{{conversationId}}/messages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

query {
  ~since: 0
}
