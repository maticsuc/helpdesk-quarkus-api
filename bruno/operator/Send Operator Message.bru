meta {
  name: Send Operator Message
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/operator/conversations/{{conversationId}}/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

body:json {
  {
    "content": "Kako vam lahko pomagam?"
  }
}
