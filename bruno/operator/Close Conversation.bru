meta {
  name: Close Conversation
  type: http
  seq: 15
}

put {
  url: {{baseUrl}}/operator/conversations/{{conversationId}}/close
  body: json
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}
