meta {
  name: List Conversations (CLOSED)
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/operator/conversations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

query {
  status: CLOSED
}
