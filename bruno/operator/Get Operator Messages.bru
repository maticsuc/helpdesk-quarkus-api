meta {
  name: Get Operator Messages
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/operator/conversations/{{conversationId}}/messages
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

query {
  ~since: 0
}
