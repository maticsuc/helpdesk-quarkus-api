meta {
  name: List Conversations (ACTIVE)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/operator/conversations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

query {
  status: ACTIVE
}
