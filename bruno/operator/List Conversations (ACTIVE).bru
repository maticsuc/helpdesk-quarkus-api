meta {
  name: List Conversations (ACTIVE)
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/operator/conversations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

query {
  status: ACTIVE
}
