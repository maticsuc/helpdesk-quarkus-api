meta {
  name: Login User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login/user
  body: json
  auth: none
}

body:json {
  {
    "username": "janez",
    "password": "password123"
  }
}

script:post-response {
  bru.setVar("token", res.body.token);
}
